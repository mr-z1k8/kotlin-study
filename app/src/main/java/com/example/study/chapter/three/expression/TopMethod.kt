package com.example.study.chapter.three.expression

/**
 * top level method. not be contained in any class. It(top level method) exists only in kotlin.
 * kotlin: use it , you can doSomething() directly.
 * java: use it , you can found a TopMethodKt.doSomething().  The compiler automatically creates TopMethodKt.java
 */
fun doSomething(){

}